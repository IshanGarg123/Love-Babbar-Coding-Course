#include<iostream>
using namespace std;


/*
TreeNode<int>* LCAinaBST(TreeNode<int>* root, TreeNode<int>* p, TreeNode<int>* q)
{
    while(root!=NULL)
    {
        if(p->data > root->data && q->data > root->data)
        {
            root = root->right;
        }
        else if(p->data < root-> data && q -> data < root->data)
        {
            root = root->left;
        }
        else
        {
            return root;
        }
    }
    return root;
}
*/

int main()
{
    cout << "Least Common Ansestor of 2 Nodes" << endl;
    return 0;
}